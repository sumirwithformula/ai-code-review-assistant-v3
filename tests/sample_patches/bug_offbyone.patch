diff --git a/example.py b/example.py
index abcdef..123456 100644
--- a/example.py
+++ b/example.py
@@ -10,4 +10,4 @@ def count_positive(numbers):
     count = 0
-    for i in range(len(numbers)+1):   # should be range(len(numbers))
+    for i in range(len(numbers)+1):
         if numbers[i] > 0:
             count += 1